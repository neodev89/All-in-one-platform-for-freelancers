import { GlobalWrapper } from "@/ui/global-wrapper/global-wrapper";

export default function LoginComponent() {
    return (
        <GlobalWrapper 
            bodyWrapped={"Login"}
        />
    )
}